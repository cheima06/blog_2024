{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

{#charger mes categories#}
    <div class="row p-5">

        <ul class="col-4 list-group ">
            {% for category in categories %}
                <li class="list-group-item"><a href="{{path('app_get_article_by_category', {id_category:category.id} )}}">{{ category.title }}"</a></li>
            {% endfor %}
        </ul>


        
        <div class="col-8 border">

            {% for article in articles %}

            <a href="{{ path('app_article_show', {id : article.id })}}">

                <div class="d-flex article p-3">

                    {% if article.picture is empty%}
                        <img class="col-md-4" src="{{ asset('build/images/default.webp') }}" alt="{{article.title}}" title="{{article.title}}">
                    {% else %}
                        <img class="col-md-4" src="{{ asset('build/images/' ~ article.picture)}}" alt="{{article.title}}" title="{{article.title}}">
                    {% endif %}
                    <div class="col-md-8 d-flex flex-column ms-3">
                        <h3>{{article.title}}</h3>
                        <p>{{article.description}}</p>
                    </div>

                </div>


            {% endfor %}

        </div>

    
    
    </div>




<ul>
 
    {#Redirection avec la fonction twig path()#}
    {#<li><a href="{{path('app_home')}}">Home</a></li>
    <li><a href="{{path('app_contact')}}">Contact</a></li>#}
    
  
    
</ul>
{% endblock %}
